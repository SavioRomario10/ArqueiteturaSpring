spring:
  application:
    name: arquiteturaspring
  datasource:
    url: jdbc:h2:mem:todo
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console